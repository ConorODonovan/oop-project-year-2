import javax.swing.*;
import java.awt.*;

public class MainContainer extends JPanel {

    // This is the panel that holds everything in the UI

    public MainContainer(Dimension size) {

        setPreferredSize(size);
        setBackground(new Color(42, 47, 78));
    }
}
